import { CustomCourseButtonDirective } from './custom-course-button.directive';

describe('CustomCourseButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomCourseButtonDirective();
    expect(directive).toBeTruthy();
  });
});
